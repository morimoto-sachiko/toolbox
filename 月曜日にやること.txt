DLL設定x64

C#　プロジェクト　→　プロパティ　32ビットのチェックを外す
AnyCPUもやめる


C++　ビルドイベント　→　ビルド後

　copy "$(TargetPath)" "..\ConsoleApp1\bin\x64\Debug\"

DLLのコピー

BATファイルがある場合のみ実行する

DLLの設定でx64にする



改行コード問題


#include <iomanip>

std::ofstream ofs("player.json");

ofs << std::setw(4) << data

setw(4)←タブ

↓バイナリ
std::ofstream ofs("test.json", std::ios::binary);